# Archived Migration Scripts

These scripts were used for the one-time Bun+PHP → Go engine cutover (Jan 2026).
The migration is complete. Kept for historical reference only.

- `cutover.sh` — Original Bun+PHP→Go migration
- `rollback.sh` — Emergency rollback to Bun+PHP
- `parity-test.sh` — Bun vs Go response comparison
