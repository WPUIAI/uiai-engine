import{describe,expect,it}from"vitest";import{evaluateAutoAddEligibility}from"../../src/lib/pairing/auto-add-eligibility";import manifest from"../../../../tests/fixtures/desktop-presentation/focusa-app-manifest.valid.json";
const hint={schema:"focusa.platform_hint.v1",daemon_url:"https://focusa.example",daemon_id:"daemon_1",device_id:"menubar_1",paired:true,client_types_seen:["menubar"],last_verified_at:"2026-08-03T00:00:00Z"};const valid={same_machine:true,same_user:true,policy_enabled:true,dismissed:false,selected_daemon_url:"https://focusa.example",hint,manifest};
describe("Menubar auto-add eligibility",()=>{it("requires every same-machine authority gate",()=>expect(evaluateAutoAddEligibility(valid)).toMatchObject({eligible:true,reason:"eligible",menubar_device_id:"menubar_1"}));it.each([["same_machine",false,"different_machine"],["same_user",false,"different_user"],["policy_enabled",false,"policy_disabled"],["dismissed",true,"dismissed"]]as const)("blocks %s",(key,value,reason)=>expect(evaluateAutoAddEligibility({...valid,[key]:value})).toMatchObject({eligible:false,reason}));it("blocks daemon mismatch and missing inherited-pairing capability",()=>{expect(evaluateAutoAddEligibility({...valid,selected_daemon_url:"https://other.example"}).reason).toBe("daemon_mismatch");expect(evaluateAutoAddEligibility({...valid,manifest:{...manifest,capabilities:manifest.capabilities.filter(value=>value!=="pair.inherited")}}).reason).toBe("missing_capability");});it("rejects secret-bearing hints",()=>expect(evaluateAutoAddEligibility({...valid,hint:{...hint,token:"no"}}).reason).toBe("invalid_hint"));});
